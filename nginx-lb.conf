events {
    worker_connections 1024;
}

http {
    upstream sso_cluster {
        server sso1:80;
        server sso2:80;
        server sso3:80;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://sso_cluster;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }
    }
}
